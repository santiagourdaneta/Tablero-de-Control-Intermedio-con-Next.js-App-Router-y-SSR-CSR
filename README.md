# 🚀 Next.js Dashboard Avanzado (SSR, CSR & Server Actions)

Este repositorio contiene una aplicación de tablero de control desarrollada con **Next.js 14+** (utilizando el App Router) que demuestra una arquitectura de complejidad intermedia al combinar el **Server-Side Rendering (SSR)** con la interactividad del **Client-Side Rendering (CSR)**

## ✨ Características Principales

* **Server-Side Rendering (SSR):** La carga inicial del listado y los detalles de usuarios se realizan en el servidor para garantizar el mejor rendimiento y SEO.
* **Client-Side Rendering (CSR):** Implementación de búsqueda y filtrado de usuarios en el navegador para una experiencia de usuario dinámica sin recargas.
* **TypeScript:** Código robusto y tipado estáticamente.

## 💻 Instalación y Uso

Asegúrate de tener **Node.js** (v18+) instalado.

1.  **Clonar el repositorio:**

    ```bash
    git clone [https://github.com/santiagourdaneta/Tablero-de-Control-Intermedio-con-Next.js-App-Router-y-SSR-CSR]
    cd [Tablero-de-Control-Intermedio-con-Next.js-App-Router-y-SSR-CSR]
    ```

2.  **Instalar dependencias:**

    ```bash
    npm install
    ```

3.  **Ejecutar en Modo Desarrollo (DEV):**

    ```bash
    npm run dev
    ```

    El sitio estará disponible en `http://localhost:3000`.